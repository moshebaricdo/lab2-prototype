# The Infinite Loader
**Do this:**
1. Find the while loop inside the `runBtn` event listener. There are comments indicating where code is missing.
2. Use AI to help you write the code to push the current crate into the `shipCargo` list and increment i so the loop doesn't run forever.
3. Run the loader sequence to verify the ship fills up to 800 tons without freezing the browser.