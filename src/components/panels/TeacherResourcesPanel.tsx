import { ScrollArea } from "../ui/scroll-area";

export function TeacherResourcesPanel() {
  return (
    <ScrollArea className="flex-1 min-h-0 p-4">
      <div className="text-sm text-muted-foreground">
        {/* Teacher resources content will go here */}
      </div>
    </ScrollArea>
  );
}
